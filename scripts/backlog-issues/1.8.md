---
task_id: "1.8"
phase: 1
phase_name: "Fix Function App"
title: "Update CORS settings (single domain: ryanmcvey.me only)"
priority: "P1 – Critical"
size: "S (half-day)"
copilot_suitable: "Yes"
labels:
  - "Phase 1 - Fix Function App"
  - "P1 – Critical"
  - "S"
  - "Copilot: Yes"
  - "area: backend"
depends_on: ["1.3"]
---

# [Phase 1] Update CORS settings (single domain: ryanmcvey.me only)

## Description

Update the Function App CORS settings to allow requests only from the single domain `ryanmcvey.me` (and its subdomains like `resume.ryanmcvey.me`). Remove any old CORS entries for .net or .cloud domains. Update both the `host.json`/`local.settings.json` and the Azure resource configuration (Bicep/ARM).

## Acceptance Criteria

- [ ] CORS allowed origins updated to `https://resume.ryanmcvey.me`
- [ ] Old .net and .cloud domain origins removed
- [ ] `host.json` CORS configuration updated
- [ ] Bicep/ARM template CORS configuration updated
- [ ] Localhost origin preserved for local development

## Dependencies

- Task 1.3: Upgrade Functions version (v3 → v4)

## Copilot Instructions Reference

Copilot can fully assist with CORS configuration updates in `host.json`, `local.settings.json`, and Bicep files.

## Notes

Only `ryanmcvey.me` and its subdomains should be in the CORS allowed origins. This is a single-domain setup.
